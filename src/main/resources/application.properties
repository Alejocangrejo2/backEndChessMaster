# ============================================
# application.properties — Configuracion Spring Boot
# ============================================
# Usa variables de entorno para produccion (Railway),
# con valores por defecto para desarrollo local.

# === Servidor ===
server.port=${PORT:8080}

# === PostgreSQL ===
# Railway provee DATABASE_URL con formato:
# postgresql://user:pass@host:port/db
# Spring Boot necesita el prefijo jdbc:
spring.datasource.url=${SPRING_DATASOURCE_URL:jdbc:postgresql://localhost:5432/chessdb}
spring.datasource.username=${SPRING_DATASOURCE_USERNAME:${DB_USERNAME:chessuser}}
spring.datasource.password=${SPRING_DATASOURCE_PASSWORD:${DB_PASSWORD:chesspass123}}
spring.datasource.driver-class-name=org.postgresql.Driver

# === JPA / Hibernate ===
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=false
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.PostgreSQLDialect
spring.jpa.properties.hibernate.format_sql=true

# === Jackson (JSON) ===
spring.jackson.serialization.write-dates-as-timestamps=false

# === JWT ===
jwt.secret=${JWT_SECRET:ChessMasterSecretKey2025SuperSecureKey256BitsMinimumRequiredHere!}
jwt.expiration=${JWT_EXPIRATION:86400000}

# === CORS ===
cors.allowed-origins=${CORS_ORIGINS:http://localhost:5173,http://localhost:3000}

# === Logging (produccion) ===
logging.level.root=${LOG_LEVEL:INFO}
logging.level.com.chess=DEBUG
